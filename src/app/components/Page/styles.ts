import { createUseStyles } from 'react-jss';

export default createUseStyles({
  root: {
    position: 'relative',
    display: 'flex',
    flexDirection: 'column',
    flex: 1,
  },
  heading: {
    textAlign: 'center',
  },
});
